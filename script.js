/**
 * Chai aur Python - Core Application Engine Structure definitions
 */
const pythonCourseDatabase = [
    {
        chapterId: 1,
        moduleTitle: "Foundations & Environment Setup",
        chapterTitle: "Introduction & Environment Setup",
        summaryNotes: "<h4>Architectural Pedigree</h4><ul><li>Python targets production runtime configurations without abstract online sandbox dependencies.</li><li>Installing native packages via <span class='code-highlight'>python.org</span> ensures deterministic compilation targets.</li><li>Windows runtimes rely on execution targets mapped simply via <span class='code-highlight'>python</span>; macOS environments map natively via explicit <span class='code-highlight'>python3</span> commands.</li></ul><h4>Operational Commands</h4><ul><li>Verify environments cleanly using: <span class='code-highlight'>python --version</span> or <span class='code-highlight'>python3 --version</span>.</li><li>Windows path bindings are clean inside Git Bash layers over default Legacy Command Prompts.</li></ul>",
        playgroundCode: "# Verification Script\nimport sys\nprint('System Python Verification Engine Initialized')\nprint(f'Runtime Core Version Targets: {sys.version}')",
        expectedOutput: "System Python Verification Engine Initialized\nRuntime Core Version Targets: 3.12.1 (tags/v3.12.1:2305ca5, Dec  7 2023, 22:03:25)",
        flashcardQuestion: "Why run Python scripts inside production terminal environments over online sandboxes?",
        flashcardAnswer: "Online compiler sandboxes strip low-level optimization contexts out entirely, blinding you to hidden cache structures like __pycache__ and preventing step-by-step local script debugging.",
        quizQuestion: "Which parameter string displays installed Python environmental targets explicitly inside structural terminal terminals?",
        quizOptions: ["python --v", "python3 -version", "python3 --version", "py -vTarget"],
        quizCorrectIndex: 2,
        quizExplanation: "Modern environment wrappers verify exact semver specifications using the double-dash explicit parameter target string natively.",
        assignmentPrompt: "Configure your local text layout wrapper. Modify the code block to track runtime parameters via python execution methods seamlessly."
    },
    {
        chapterId: 2,
        moduleTitle: "Foundations & Environment Setup",
        chapterTitle: "Python Inner Architecture Engine",
        summaryNotes: "<h4>Under the Hood Pipeline Lifecycle</h4><ul><li>Python transitions components via two core architectural pipelines: <b>Compilation Phase</b> followed directly by the <b>PVM Runtime Engine</b> loop.</li><li>Source files (.py) parse down cleanly into localized low-level Intermediate Bytecode structures stored inside optimized <span class='code-highlight'>__pycache__/</span> blocks as <span class='code-highlight'>.pyc</span> structures.</li><li>Bytecode patterns are platform-independent targets, optimized for internal execution efficiency rather than machine hardware architectures directly.</li></ul><h4>The Python Virtual Machine (PVM)</h4><ul><li>The PVM functions as a continuous evaluation engine runtime wrapper loop.</li><li>It reads intermediate bytecode arrays sequentially, compiling instructions JIT into machine-level runtime signals.</li><li><b>Crucial:</b> Python variants track compilation defaults natively—Standard systems run <b>CPython</b> (compiled using C extensions), while high-performance engines map via <b>PyPy</b> execution layers.</li></ul>",
        playgroundCode: "# Simulate internal bytecode caching state indicators\ndef evaluate_pipeline():\n    print('Source Layer (.py) parsed to Bytecode intermediate states')\n    print('Caching into hidden __pycache__ mapping objects as .pyc targets')\n\nevaluate_pipeline()",
        expectedOutput: "Source Layer (.py) parsed to Bytecode intermediate states\nCaching into hidden __pycache__ mapping objects as .pyc targets",
        flashcardQuestion: "What functional criteria determines if Python compiles source strings into hidden __pycache__ files?",
        flashcardAnswer: "__pycache__ directories only cache imported modular source structures. top-level running scripts bypass file-level caching arrays to optimize memory allocation dynamically.",
        quizQuestion: "What is the primary target execution file designation generated by the bytecode layout layer within CPython frameworks?",
        quizOptions: [".pyobj", ".pyc", ".pvmexe", ".dll"],
        quizCorrectIndex: 1,
        quizExplanation: "The .pyc extension explicitly indicates compiled, optimized internal bytecode configurations generated during runtime translation checks.",
        assignmentPrompt: "Write an interactive module tracking bytecode translation triggers. Output how changing variable states handles structural backward caching indicators."
    },
    {
        chapterId: 3,
        moduleTitle: "Foundations & Environment Setup",
        chapterTitle: "Python Interactive Shell Fundamentals",
        summaryNotes: "<h4>REPL Execution Dynamics</h4><ul><li>The Python shell functions as an immediate structural evaluation tool, following a Read-Eval-Print Loop layout pattern.</li><li>It immediately executes each parsed structural instruction block sequentially, without intermediate manual optimization sweeps.</li><li>Ideal for runtime prototyping operations, module inspection checks, and identifying code error indicators quickly.</li></ul><h4>Core Limitations</h4><ul><li>Session memory context is strictly ephemeral. Closing shell instances flushes out variable structures and layout matrices completely.</li><li><b>Pro-Tip:</b> Exit structural instances properly via explicit <span class='code-highlight'>exit()</span> or <span class='code-highlight'>Ctrl + D</span> targets over volatile suspension hooks (<span class='code-highlight'>Ctrl + Z</span>).</li></ul>",
        playgroundCode: ">>> score = 100\n>>> score * 4\n400\n>>> print('Interactive REPL Execution simulation completed.')",
        expectedOutput: "400\nInteractive REPL Execution simulation completed.",
        flashcardQuestion: "What distinguishes REPL evaluations from traditional source script executions?",
        flashcardAnswer: "The REPL environment prints string outputs for evaluated expressions immediately without requiring explicit print statements, whereas standalone source scripts require systematic print wrapping commands.",
        quizQuestion: "Which structural command cleanly resets terminal sessions out of active Python REPL execution boundaries?",
        quizOptions: ["quit", "terminate()", "exit()", "sys.flush()"],
        quizCorrectIndex: 2,
        quizExplanation: "Executing the core built-in method exit() signals standard termination vectors to the outer interpreter wrapper cleanly.",
        assignmentPrompt: "Simulate a live math parser script sequence. Instantiate variables directly and use expression multipliers to show how the REPL layer handles calculations instantly."
    },
    {
        chapterId: 4,
        moduleTitle: "Memory Dynamics & Objects",
        chapterTitle: "Mutable vs Immutable Principles",
        summaryNotes: "<h4>Underlying Reference Architecture</h4><ul><li>Python manages data structures entirely as encapsulated <b>Object Identifiers</b>. Variables do not hold literal raw data; they function exclusively as memory reference labels.</li><li><b>Immutable Types:</b> Objects whose memory state cannot be updated after instantiation (<span class='code-highlight'>int</span>, <span class='code-highlight'>float</span>, <span class='code-highlight'>str</span>, <span class='code-highlight'>tuple</span>).</li><li><b>Mutable Types:</b> Data structures allowing in-place memory state modifications (<span class='code-highlight'>list</span>, <span class='code-highlight'>dict</span>, <span class='code-highlight'>set</span>).</li></ul><h4>Memory Overlap & Reallocation Loops</h4><ul><li>Assigning <span class='code-highlight'>x = 10</span> followed by <span class='code-highlight'>y = x</span> binds both reference labels to the same numerical object target.</li><li>Updating an immutable reference with <span class='code-highlight'>x = 15</span> does not alter the original memory block; it binds the variable label to a completely new object location.</li><li><b>Garbage Collection Tracking:</b> Objects whose reference metrics drop to zero are systematically reaped by the automated reference-counting engine.</li></ul>",
        playgroundCode: "# Memory Reference Inspection Simulation\na = [1, 2, 3]\nb = a\nprint(f'Identity Match: {a is b}')\na.append(4)\nprint(f'Mutation Impact on b: {b}')",
        expectedOutput: "Identity Match: True\nMutation Impact on b: [1, 2, 3, 4]",
        flashcardQuestion: "Why do string variables appear alterable despite being explicitly classified as immutable?",
        flashcardAnswer: "String modification operations don't mutate the underlying array. Instead, they instantiate a brand-new string object in memory and rebind the existing label to this new location.",
        quizQuestion: "What explicit analytical checking parameter verifies if two discrete layout labels reference the exact same underlying memory block?",
        quizOptions: ["==", "is", "id.match()", "compare()"],
        quizCorrectIndex: 1,
        quizExplanation: "The 'is' identity operator checks if both references point to the exact same underlying memory address block, whereas '==' compares only logical contents.",
        assignmentPrompt: "Instantiate a shared numerical identifier array. Track modification outcomes across structural labels to show how Python handles mutation constraints dynamically."
    },
    {
        chapterId: 5,
        moduleTitle: "Memory Dynamics & Objects",
        chapterTitle: "Numbers, Booleans & Math Operations",
        summaryNotes: "<h4>Precision Mechanics & Numeric Sets</h4><ul><li>Python integrates specialized, high-precision storage components capable of managing large integer boundaries without risking arithmetic overflow.</li><li><b>Floating Point Realities:</b> Binary fraction limits can introduce subtle precision variations (e.g., <span class='code-highlight'>0.1 + 0.1 + 0.1</span> can yield unexpected trailing bits like <span class='code-highlight'>0.30000000000000004</span>).</li><li>Use the specialized <span class='code-highlight'>decimal</span> module for applications requiring high structural precision, such as financial transaction engines.</li></ul><h4>Sets & Special Numeric Systems</h4><ul><li><b>Sets:</b> Unordered collections enforcing absolute item uniqueness via memory hashing protocols. Highly optimized for operations like intersection (<span class='code-highlight'>&</span>) and union (<span class='code-highlight'>|</span>).</li><li><b>Boolean Equivalency:</b> Booleans behave internally as customized numeric extensions: <span class='code-highlight'>True == 1</span> and <span class='code-highlight'>False == 0</span> are valid operational targets inside arithmetic matrices.</li></ul>",
        playgroundCode: "from decimal import Decimal\n\nval1 = 0.1 + 0.1 + 0.1\nval2 = Decimal('0.1') + Decimal('0.1') + Decimal('0.1')\nprint(f'Standard Float Output: {val1}')\nprint(f'Decimal Engine Output: {val2}')",
        expectedOutput: "Standard Float Output: 0.30000000000000004\nDecimal Engine Output: 0.3",
        flashcardQuestion: "How does Python handle precision checking limits for complex mathematical transformations internally?",
        flashcardAnswer: "Python relies on C-level double-precision floating-point formats natively. For arbitrary precision requirements, it routes calculations through the specialized 'decimal' or 'fractions' modules.",
        quizQuestion: "Which operator configuration isolates structural mathematical remainder definitions natively inside numeric processing scripts?",
        quizOptions: ["/", "//", "%", "^"],
        quizCorrectIndex: 2,
        quizExplanation: "The modulo (%) operator divides the target number and returns only the remaining fractional values.",
        assignmentPrompt: "Create a script using mathematical powers (<span class='code-highlight'>**</span>) that checks floating-point accuracy limits and resolves calculation drift using specialized precision functions."
    },
    {
        chapterId: 6,
        moduleTitle: "Memory Dynamics & Objects",
        chapterTitle: "Strings & Text Manipulation Engine",
        summaryNotes: "<h4>Slicing, Dicing, and Step Controls</h4><ul><li>Strings function as immutable sequential character arrays containing optimized positional indexing chains.</li><li>Advanced slicing formatting models use a <span class='code-highlight'>[start:stop:step]</span> syntax specification target loop.</li><li>Setting a negative step value like <span class='code-highlight'>[::-1]</span> reverses the target string index structure instantly.</li></ul><h4>Formatting & Escape Sequences</h4><ul><li>Prefixing text paths with <span class='code-highlight'>r</span> instantiates a <b>Raw String</b> context, which deactivates evaluation loops for character configurations like escape sequences (<span class='code-highlight'>\n</span>).</li><li>Modern workflows construct structured dynamic text strings via <b>f-strings</b> (formatted string literals) rather than legacy concatenation operations.</li></ul>",
        playgroundCode: "path_sample = r'C:\\users\\new_project\\target\\notes.txt'\nslice_demo = 'Chai_and_Code'\nprint(f'Raw Path Parsing outcome: {path_sample}')\nprint(f'Slicing Execution (Hops of 2): {slice_demo[0:7:2]}')",
        expectedOutput: "Raw Path Parsing outcome: C:\\users\\new_project\\target\\notes.txt\nSlicing Execution (Hops of 2): Ci_n",
        flashcardQuestion: "What is the computational benefit of using the string join() method over traditional iterative concatenation (+) loops?",
        flashcardAnswer: "Traditional concatenation creates a brand-new copy of the string object on every iteration, leading to O(N^2) memory overhead. The join() method calculates total memory allocation up front, rendering structural text stitching at linear O(N) efficiency.",
        quizQuestion: "What character sequence generates raw execution states, neutralizing escape sequences inside complex system strings?",
        quizOptions: ["f", "b", "r", "u"],
        quizCorrectIndex: 2,
        quizExplanation: "The 'r' prefix instructs the parser engine to interpret backslashes as literal text characters, bypassing standard escape sequence conversions like tabs or newlines.",
        assignmentPrompt: "Build a string parser utility script. Use slicing notation with customized step intervals to isolate specific sub-characters from input text strings."
    },
    {
        chapterId: 7,
        moduleTitle: "Collection Data Structures",
        chapterTitle: "Lists & Array Architecture",
        summaryNotes: "<h4>Mutation Mechanics & List Memory Layout</h4><ul><li>Lists operate as mutable ordered reference containers. While the list wrapper maintains a stable pointer address, its contents can be altered dynamically.</li><li>Modifying list structures directly inside shared tracking configurations affects all down-stream variable targets pointing to that reference.</li><li><b>Decoupling Memory Arrays:</b> Use <span class='code-highlight'>.copy()</span> or explicit full-slicing strings (<span class='code-highlight'>[:]</span>) to isolate tracking adjustments and maintain separate memory states cleanly.</li></ul><h4>List Comprehensions</h4><ul><li>List comprehensions provide a clean, optimized syntax loop formatting structure: <span class='code-highlight'>[expression for item in iterable]</span>.</li><li>They run at optimized speed targets directly inside CPython implementation wrappers, executing faster than manual append loop strategies.</li></ul>",
        playgroundCode: "# List Comprehension vs Pointer Synchronization checks\nbase_matrix = [1, 2, 3]\nsandboxed_copy = base_matrix.copy()\nsandboxed_copy.append(99)\n\ncomprehension_demo = [x**2 for x in range(5)]\nprint(f'Original Matrix State: {base_matrix}')\nprint(f'Comprehension Logic Array: {comprehension_demo}')",
        expectedOutput: "Original Matrix State: [1, 2, 3]\nComprehension Logic Array: [0, 1, 4, 9, 16]",
        flashcardQuestion: "What is the structural difference between the list append() and extend() methods?",
        flashcardAnswer: "The append() method adds its argument to the end of the list as a single, nested object. The extend() method iterates over its collection argument, appending each individual item sequentially.",
        quizQuestion: "Which operational configuration creates a completely separate copy of a list, ensuring adjustments don't overwrite the original source data?",
        quizOptions: ["new_list = old_list", "new_list = old_list.copy()", "new_list = list(old_list.id)", "new_list = old_list.extend()"],
        quizCorrectIndex: 1,
        quizExplanation: "The .copy() method generates a shallow copy, allocating a distinct memory block for the outer list and decoupling downstream tracking references cleanly.",
        assignmentPrompt: "Write a script that filters tracking records using list comprehensions. Isolate specific values and apply numeric formatting to the output strings."
    },
    {
        chapterId: 8,
        moduleTitle: "Collection Data Structures",
        chapterTitle: "Dictionaries & Key-Value Management",
        summaryNotes: "<h4>Hashing Mechanics & Structural Lookups</h4><ul><li>Dictionaries operate as mutable collections that map unique search keys to specific data values. They do not rely on fixed numerical sequence indices.</li><li>Lookups operate via underlying memory hashing layers, yielding optimal runtime efficiency ($O(1)$ search latency).</li><li><b>Advanced Extraction Patterns:</b> Use the safe access method <span class='code-highlight'>.get()</span> to retrieve values without risking standard missing key exceptions.</li></ul><h4>Dictionary Comprehension & Traversal</h4><ul><li>Loop over active mappings cleanly using specialized traversal methods like <span class='code-highlight'>.items()</span>, <span class='code-highlight'>.keys()</span>, and <span class='code-highlight'>.values()</span>.</li><li>Construct updated key-value structures instantly via inline statements: <span class='code-highlight'>{k:v for k,v in iterable}</span>.</li></ul>",
        playgroundCode: "# Dictionary Processing Simulation\norder_sheet = {'item_type': 'Masala_Chai', 'tier': 'Premium'}\n\n# Safe checking operation\nprint(f\"Safe retrieval result: {order_sheet.get('qty', 0)}\")\n\n# Inline generation loops template\nsquare_map = {x: x**2 for x in range(3)}\nprint(f'Generated Mapping: {square_map}')",
        expectedOutput: "Safe retrieval result: 0\nGenerated Mapping: {0: 0, 1: 1, 2: 4}",
        flashcardQuestion: "What happens when you invoke dictionary lookups using traditional brackets syntax for keys that do not exist?",
        flashcardAnswer: "Traditional bracket lookups throw a terminal 'KeyError' exception when encountering unmapped keys. The safer option, .get(), handles missing lookups gracefully by returning a configured default value instead.",
        quizQuestion: "Which built-in loop extension method isolates both keys and values cleanly during dictionary processing sweeps?",
        quizOptions: [".keys()", ".values()", ".items()", ".entry_set()"],
        quizCorrectIndex: 2,
        quizExplanation: "The .items() method iterates over key-value pairs, returning them as unpackable tracking tuples on each pass.",
        assignmentPrompt: "Build a lookup dictionary tracking configuration parameters. Loop through the records, extract values using safe methods, and modify selected text strings."
    },
    {
        chapterId: 9,
        moduleTitle: "Collection Data Structures",
        chapterTitle: "Tuples & Sequence Sequences",
        summa